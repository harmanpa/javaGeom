<settings>
    <servers>
        <server>
            <id>github</id>
            <username>${env.GITHUB_USERNAME}</username>
            <password>${env.GITHUB_PASSWORD}</password>
        </server>
        <server>
            <id>bintray-harmanpa-caetechpublic</id>
            <username>harmanpa</username>
            <password>${env.BINTRAY_PASSWORD}</password>
        </server>
        <server>
            <id>dist</id>
            <username>dist</username>
            <password>${env.DIST_PASSWORD}</password>
        </server>
    </servers>
</settings>
